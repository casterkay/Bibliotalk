services:
  ingestion:
    image: astral/uv:python3.12-trixie-slim
    working_dir: /workspace
    env_file:
      - .env
    environment:
      BIBLIOTALK_DB_PATH: /data/bibliotalk.db
      UV_PROJECT_ENVIRONMENT: /data/venv-ingestion
    volumes:
      - ../..:/workspace
      - bibliotalk_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - uv
      - run
      - --package
      - bt_cli
      - bibliotalk
      - collector
      - run
      - --agent
      - ${BIBLIOTALK_AGENT}

  agents:
    image: astral/uv:python3.12-trixie-slim
    working_dir: /workspace
    env_file:
      - .env
    environment:
      AGENTS_SERVICE_URL: http://agents:8009
      BIBLIOTALK_DB_PATH: /data/bibliotalk.db
      UV_PROJECT_ENVIRONMENT: /data/venv-agents
    volumes:
      - ../..:/workspace
      - bibliotalk_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${AGENTS_PORT:-8009}:8009"
    command:
      - uv
      - run
      - --with
      - uvicorn
      - --package
      - agents_service
      - uvicorn
      - agents_service.server:app
      - --host
      - 0.0.0.0
      - --port
      - "8009"

  voip:
    image: node:22-bookworm
    working_dir: /workspace/services/voip_service
    env_file:
      - .env
    environment:
      AGENTS_SERVICE_URL: http://agents:8009
      VOIP_SERVICE_HOST: 0.0.0.0
      VOIP_SERVICE_PORT: 9012
    volumes:
      - ../..:/workspace
      - voip_node_modules:/workspace/services/voip_service/node_modules
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${VOIP_PORT:-9012}:9012"
    command:
      - sh
      - -lc
      - npm install && npm start
    depends_on:
      - agents

  discord:
    image: astral/uv:python3.12-trixie-slim
    working_dir: /workspace
    env_file:
      - .env
    environment:
      VOIP_SERVICE_URL: http://voip:9012
      AGENTS_SERVICE_URL: http://agents:8009
      BIBLIOTALK_DB_PATH: /data/bibliotalk.db
      UV_PROJECT_ENVIRONMENT: /data/venv-discord
    volumes:
      - ../..:/workspace
      - bibliotalk_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - uv
      - run
      - --package
      - bt_cli
      - bibliotalk
      - discord
      - run
    depends_on:
      - ingestion
      - agents
      - voip

  memories:
    image: astral/uv:python3.12-trixie-slim
    working_dir: /workspace
    env_file:
      - .env
    environment:
      BIBLIOTALK_DB_PATH: /data/bibliotalk.db
      UV_PROJECT_ENVIRONMENT: /data/venv-memories
    volumes:
      - ../..:/workspace
      - bibliotalk_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${MEMORIES_PORT:-8080}:${MEMORIES_PORT:-8080}"
    command:
      - uv
      - run
      - --package
      - bt_cli
      - bibliotalk
      - memories
      - run
      - --host
      - ${MEMORIES_HOST:-0.0.0.0}
      - --port
      - ${MEMORIES_PORT:-8080}

  webui:
    image: astral/uv:python3.12-trixie-slim
    working_dir: /workspace
    env_file:
      - .env
    environment:
      BIBLIOTALK_DB_PATH: /data/bibliotalk.db
      MEMORIES_SERVICE_URL: http://memories:${MEMORIES_PORT:-8080}
      UV_PROJECT_ENVIRONMENT: /data/venv-webui
    volumes:
      - ../..:/workspace
      - bibliotalk_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${WEBUI_PORT:-8090}:8090"
    command:
      - uv
      - run
      - --package
      - bt_cli
      - bibliotalk
      - webui
      - run
      - --host
      - 0.0.0.0
      - --port
      - "8090"
    depends_on:
      - memories

volumes:
  bibliotalk_data:
  voip_node_modules:
